apiVersion: v1
kind: Service
metadata:
  name: cicd-demo-service
  namespace: cicd-demo
spec:
  selector:
    app: cicd-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer